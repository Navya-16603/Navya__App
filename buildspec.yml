version: 0.2

phases:
  install:
    commands:
      - echo "Install phase started"
  build:
    commands:
      - echo "Build phase - No build needed, static files only"
  post_build:
    commands:
      - echo "Build completed successfully"

artifacts:
  files:
    - '**/*'
