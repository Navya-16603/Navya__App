version: 0.2
phases:
  install:
    commands:
      - chmod +x scripts/clean.sh
      - chmod +x scripts/start_server.sh
      - echo "Installing dependencies..."
  build:
    commands:
      - echo "Build complete"
artifacts:
  files:
    - '**/*'
