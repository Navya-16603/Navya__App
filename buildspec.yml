version: 0.2

phases:
  install:
    commands:
      - echo "Setting permissions..."
      - chmod +x scripts/clean.sh
      - chmod +x scripts/start_server.sh

  build:
    commands:
      - echo "Build completed."
artifacts:
  files:
    - '**/*'
   

